<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>nz.co.aetheric.parent</groupId>
	<artifactId>maven-parent-global</artifactId>
	<version>1.1.0</version>
	<packaging>pom</packaging>

	<name>Maven Parent: Global</name>
	<description>The organisation-level parent for all Aetheric Engineering modules.</description>
	<url>https://github.com/aetheric/maven-parent/tree/maven-parent-global</url>
	
	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
	</properties>
	
	<build>
		<pluginManagement>
			<plugins>

				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-scm-plugin</artifactId>
					<version>1.8.1</version>
				</plugin>

				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-enforcer-plugin</artifactId>
					<version>1.2</version>
					<configuration>
						<rules>

							<requireMavenVersion>
								<version>[3.0.4,)</version>
							</requireMavenVersion>

							<!--  Don't allow releases with snapshop deps or parents -->
							<requireReleaseDeps>
								<message>No Snapshots Allowed!</message>
								<failWhenParentIsSnapshot>true</failWhenParentIsSnapshot>
								<searchTransitive>true</searchTransitive>
							</requireReleaseDeps>

							<!-- Don't allow accidental inclusion of commons logging or log4j -->
							<bannedDependencies>
								<excludes>
									<exclude>commons-logging</exclude>
									<exclude>commons-logging-api</exclude>
								</excludes>
							</bannedDependencies>

						</rules>
						<fail>true</fail>
					</configuration>
				</plugin>

				<plugin>
					<artifactId>maven-release-plugin</artifactId>
					<version>2.4.1</version>
					<configuration>
						<mavenExecutorId>forked-path</mavenExecutorId>
						<preparationGoals>clean enforcer:enforce verify</preparationGoals>
						<goals>enforcer:enforce deploy</goals>
						<tagBase>release/${project.groupId}/</tagBase>
						<pushChanges>false</pushChanges>
						<localCheckout>true</localCheckout>
					</configuration>
				</plugin>

				<!-- default to Java 7 -->
				<plugin>
					<artifactId>maven-compiler-plugin</artifactId>
					<configuration>
						<source>1.7</source>
						<target>1.7</target>
					</configuration>
				</plugin>

				<plugin>
					<groupId>org.codehaus.mojo</groupId>
					<artifactId>build-helper-maven-plugin</artifactId>
					<version>1.8</version>
				</plugin>

			</plugins>
		</pluginManagement>
	</build>
	
	<profiles>

		<profile>
			<id>sonaytpe</id>
			<activation>
				<property>
					<name>performRelease</name>
					<value>true</value>
				</property>
			</activation>
			<build>
				<plugins>

					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-gpg-plugin</artifactId>
						<version>1.4</version>
						<executions>

							<execution>
								<id>sign-artifacts</id>
								<phase>verify</phase>
								<goals>
									<goal>sign</goal>
								</goals>
							</execution>

						</executions>
					</plugin>

				</plugins>
			</build>
		</profile>

	</profiles>

	<licenses>
		<license>
			<name>MIT</name>
			<url>http://opensource.org/licenses/mit</url>
		</license>
	</licenses>

	<developers>
		<developer>
			<id>tzrlk</id>
			<name>Peter Cummuskey</name>
			<url>http://gplus.to/tzrlk</url>
			<email>peterc@aetheric.co.nz</email>
			<timezone>UTC+12</timezone>
		</developer>
	</developers>

	<issueManagement>
		<system>github</system>
		<url>https://github.com/aetheric/maven-parent/issues</url>
	</issueManagement>

	<scm>
		<url>https://github.com/aetheric/maven-parent</url>
		<connection>scm:git:git://github.com/aetheric/maven-parent.git</connection>
		<developerConnection>scm:git:git@github.com:aetheric/maven-parent.git</developerConnection>
		<tag>maven-parent-global-1.1.0</tag>
	</scm>
	
	<distributionManagement>

		<repository>
			<id>sonatype-staging</id>
			<name>oss.sonatype.org Staging Repository</name>
			<url>http://oss.sonatype.org/service/local/staging/deploy/maven2/</url>
		</repository>

		<snapshotRepository>
			<id>sonatype-snapshots</id>
			<name>oss.sonatype.org Snapshot Repository</name>
			<url>https://oss.sonatype.org/content/repositories/snapshots/</url>
		</snapshotRepository>

	</distributionManagement>

</project>